{
  "id": "json-syntax-invalid",
  "language": "json",
  "support": "supported",
  "kind": "invalid",
  "description": "A trailing comma and an unquoted key — the defects ADR-025 lists for Tier B JSON. The JSON validator reports the first structural error (the unquoted `version` key on line 3).",
  "requiresTools": [],
  "ignoreRules": [],
  "expected": [
    {
      "file": "config.json",
      "line": 3,
      "ruleId": "json-parse",
      "severity": "error",
      "analyzer": "json",
      "repairAvailable": false,
      "note": "JSON.parse rejects the unquoted property name `version`. No deterministic autofix yet, so this is classified ai-required — a model can quote the key, verified before Apply."
    }
  ]
}
