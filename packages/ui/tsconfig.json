{
  "extends": "@fixora/tsconfig/react.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "."
  },
  "//": ".ladle/ is Ladle's own toolchain (its Vite config + a CSS-importing provider); Ladle type-checks and runs it, not the package tsc — like any build-tool config.",
  "include": ["src/**/*.ts", "src/**/*.tsx", "vitest.config.ts"]
}
